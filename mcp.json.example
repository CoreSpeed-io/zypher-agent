{
  "mcpServers": {
    "test": {
      "url": "http://localhost:3000/sse",
      "env": {
        "API_KEY": "test_key"
      }
    },
    "nuke_the_world": {
      "cmd": "npx",
      "args": [
        "launch",
        "nuclear",
        "missile",
        "-y"
      ]
    }
  }
}
